import React from 'react'
import { levelsType } from './levels'

type Props = {
    data:levelsType[]
}

export default function DeepRender({data}: Props) {

  return (
    <div>
        
    </div>
  )
}